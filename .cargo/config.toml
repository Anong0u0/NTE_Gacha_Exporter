[alias]
agent = "run --manifest-path tools/agent-smoke/Cargo.toml --"
check-long-code = "xtask check-long-code"
ci = "xtask ci"
smoke = "run --manifest-path tools/agent-smoke/Cargo.toml -- smoke"
xtask = "run --package xtask --"
